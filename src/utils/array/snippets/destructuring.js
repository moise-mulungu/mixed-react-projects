const user = {
  name: 'Moise theMan',
  city: 'Goma',
  province: 'North Kivu',
  country: 'Congo',
  postalCode: 'A1B2C3',
}

const { name, country } = user

console.log(name) // ‘Moise theMan’
console.log(country) // ‘Congo’
