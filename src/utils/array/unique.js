// There is a lodash function uniq
export default function getUniqueItemInArray(array) {
  return [...new Set(array)]
}

//(done) DM: todoMM: turn it into a function and put in the parent directory (also, in the new file, mention that there is a lodash function uniq)
