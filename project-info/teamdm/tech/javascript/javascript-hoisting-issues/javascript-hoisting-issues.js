// I installed tsx paakage because it allows run js (and ts) scripts from the command line without worrying about commonjs VS esmodules and don't need to use the .mjs extension in NextJS repos

// (done)DM: todoMM: add a directory of same name as this file, and put each section in its own .js file WHY so we can run each and because each concept has more to it, so it is worthwhile to have a separate file on each.
